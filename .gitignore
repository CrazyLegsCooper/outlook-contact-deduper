# === Personal data — NEVER commit ===
# Contact backups the app downloads and any exported contact data
contacts-backup-*.json
*-contacts-*.json
exports/
backups/
*.contacts.json
# iCloud / Apple Contacts vCard exports
*.vcf
# Playwright working dir holds screenshots + a real contacts backup
.playwright-mcp/
# Stray Playwright screenshots (may contain real contact data)
/authgate.png
/passkey-prompt.png
/very-likely-review.png

# Real app config containing your Entra client ID (use config.example.ts as the template)
src/config.ts
config.local.*
.env
.env.*
!.env.example

# === Node / Vite ===
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Test / coverage output
coverage/

# === Editor / OS ===
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
